# Ignore everything
*

# But do not ignore these specific files
!agent.py
!requirements.txt
!readme.md

# Do not ignore directories, so we can find the files
!*/

# Do not ignore the .gitignore file itself
!.gitignore
